# ── Build / simulation artifacts ──────────────────────────────────────────
sim
sim_*
selftest_sim
validate_chowdsp.exe
*.vcd
sim.vvp

# ── Generated text output (regenerated by running sims) ──────────────────
*_output.txt
tb_output.txt
top_tb_output.txt
fullchain_output.txt
chowdsp_output.txt

# ── WAV test outputs (regenerated by test_triode.py) ─────────────────────
wav_*.wav

# ── Synthesis intermediates (Gowin) ──────────────────────────────────────
# Keep project.fs (constraints) and project.rpt.txt (reports)
fpga/impl/gwsynthesis/
fpga/impl/pnr/*.db
fpga/impl/pnr/*.log
fpga/impl/pnr/*.timing_paths
fpga/impl/pnr/*.html

# ── Node modules ─────────────────────────────────────────────────────────
ui/node_modules/

# ── Caches ───────────────────────────────────────────────────────────────
synth_cache/
__pycache__/
*.pyc

# ── Test results ─────────────────────────────────────────────────────────
test_results.json

# ── NOTE: hex LUT files (ip_lut*.hex, dip_d*.hex, cab_ir.hex) ───────────
# These CAN be regenerated by tube_lut_gen.py, but are kept tracked because
# Gowin synthesis needs them present at build time.
